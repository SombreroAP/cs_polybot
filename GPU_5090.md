# 5090 RTX — Training & Inference Box

Living doc. I update this every session so future-me (or a fresh context) can pick up without re-discovering.

## Access (confirmed working 2026-04-24)
- **SSH**: `ssh Andre@192.168.76.196` (passwordless key auth from this Mac — already set up)
- **Ollama HTTP**: `http://192.168.76.196:11434` (open on LAN, referenced in `.env` as `OLLAMA_URL`)
- Also used by `live_dashboard.py` (GPU polling), `telegram_admin.py` (`/gpu` command), `_remote/training_watchdog.py`
- User/OS: Windows, user `Andre`

## Hardware (verified)
- GPU: **RTX 5090, driver 596.21, 32,607 MiB total, 23,021 MiB in use** (qwen3:30b-a3b loaded, see below)
- CUDA toolkit: **13.2 (V13.2.51)** — bleeding edge, forward-compatible with 12.x runtimes
- Python launcher: `py -3.11` → Python 3.11.9 (usable for ML stack)
- Python 3.14.3 is the default `python` — too new, no ML wheels. Always use `py -3.11` for training work.
- Disks: C: 144GB free, D: 59GB free, **E: 1.3TB free** ← use for training, F: 1.5TB free, G: 137GB, H: 358GB, I: 730GB

## Current software state
| Component | State | Notes |
|---|---|---|
| Ollama 0.21.2 | ✅ running | `C:\Users\Andre\AppData\Local\Programs\Ollama\ollama.exe` |
| PyTorch (py 3.11) | ❌ CPU-only (`2.10.0+cpu`) | **Needs reinstall with CUDA build** |
| Unsloth | ⚠️ installed but dead | throws `Unsloth cannot find any torch accelerator` because torch is CPU |
| CUDA driver | ✅ 596.21 (CUDA 13.2 runtime) | supports PyTorch cu128 wheels (Blackwell sm_120) |

### Ollama models available on the box (as of 2026-04-24)
All Q4_K_M unless noted:
- `qwen3:30b-a3b` (18.5 GB) — **currently loaded, keep_alive 24h**, expires `2026-04-25T18:48:05` (production model)
- `qwen3:30b-a3b-think`
- `qwen3.6:35b-a3b` (24 GB)
- `qwen2.5:32b-instruct-q4_K_M`
- `qwen2.5:14b-instruct-q4_K_M`
- `qwen2.5-coder:32b`, `qwen2.5-coder:7b`
- `deepseek-r1:32b`, `deepseek-r1:14b`, `deepseek-r1:8b`
- `llama3.3:70b-instruct-q4_K_M` (42 GB)
- `llama3.1:8b-instruct-q4_K_M`
- `mistral-small3.2:24b`, `phi4:14b`, `gemma4:latest`

### Session log
- **2026-04-24 15:48 UTC**: Discovered box was already linked in codebase. Confirmed SSH + ollama access.
- **2026-04-24 15:49 UTC**: Warm-loaded `qwen3:30b-a3b` with `keep_alive: "24h"` — sits in VRAM, first call to edge_analyst should now be ~1-2s instead of 60-90s cold-load. **This alone should unblock shadow trading.**
- **2026-04-24 15:50 UTC**: Surveyed hardware + software state. Discovered PyTorch is CPU-only on Python 3.11; Unsloth can't see the GPU. This is the blocker for local training.
- **2026-04-24 15:55 UTC**: Box went offline — ping 100% loss, SSH + ollama unreachable. Paused torch reinstall. Warm-loaded model will survive the outage (already in VRAM) but if the box fully reboots we'll need to re-warm on return.
- **2026-04-24 16:10 UTC**: Diagnosed outage — my Mac had been reaching the 5090 via a dead WireGuard tunnel (`utun4`, 10.5.0.2). My Mac then moved to 192.168.76.185 (same LAN as 5090), direct route works. PC was rebooted during the outage — ollama model cache was flushed. Re-warmed `qwen3:30b-a3b` (37.7s cold-load, 24h keep-alive).
- **2026-04-24 16:12 UTC**: Uninstalled CPU torch (2.10.0+cpu, torchvision 0.25.0, torchaudio 2.6.0+cu124) and started `pip install --pre torch torchvision --index-url .../nightly/cu128`. ~3GB download in background.
- **2026-04-24 16:25 UTC**: Torch installed: **torch 2.12.0.dev20260408+cu128**, torchvision 0.27.0.dev. `torch.cuda.is_available() == True`, `get_device_name(0) == "NVIDIA GeForce RTX 5090"`, `cuda_ver == 12.8`, `capability == (12, 0)` (Blackwell sm_120). 🎯 **Foundation live.**
- **2026-04-24 16:26 UTC**: Pip warns unsloth 2026.4.4 / unsloth_zoo 2026.4.6 pin `torch<2.11` — incompatible with our torch 2.12 nightly. Upgrading both from GitHub main with `--no-deps` so they don't re-downgrade torch. In background.
- **2026-04-24 16:27 UTC**: Copied `data/training/sft.jsonl` (136 MB, 10,125 examples) → `E:\training\sft_clean.jsonl` on the 5090 via scp. Training workspace ready.
- **2026-04-24 16:30 UTC**: `pip install -U git+unsloth/...` landed unsloth **2026.4.8** + unsloth_zoo **2026.4.9** with `--no-deps` (protects torch 2.12 pin).
- **2026-04-24 16:33 UTC**: 🎯 **Full-stack smoke load succeeded.** `FastLanguageModel.from_pretrained("unsloth/Qwen2.5-14B-Instruct-bnb-4bit")` loads all 579 shards in 9s, uses 9.5 GB / 31.84 GB VRAM. Bfloat16 TRUE. Platform Windows, Triton 3.2.0. FA2 broken → Xformers fallback (slower but fine; can fix later with `pip install flash-attn --no-build-isolation`).
- **2026-04-24 16:34 UTC**: Finding: VPS `85.137.174.57` CANNOT reach `192.168.76.196` (private LAN, no tunnel). The edge_analyst ollama timeouts from VPS were unfixable from my side — need either Tailscale or set VPS to `ANTHROPIC_API_KEY`-only per `TRAINING_GUIDE.md`. Removed the useless VPS cron. Warm-load on 5090 is still useful for **local dev** (evaluator.py, backtests, shootouts).
- **2026-04-24 16:35 UTC**: Ported `_remote/real_train_weighted.py` → `_remote/train_5090.py` (E:\training paths, added `--max-steps` arg). Copied to 5090. Launched 50-step smoke training in background.
- **2026-04-24 16:38 UTC**: Smoke train **failed at step 0** with `LLVM ERROR: Cannot select: intrinsic %llvm.nvvm.shfl.sync.bfly.i32` and `'sm_120' is not a recognized processor for this target`. Cause: **Triton-windows 3.2.0.post19 predates Blackwell**. Dataset pipeline + LoRA attach all worked fine (17,126 examples after 8× buy oversample, 137 M trainable params on 14.9 B frozen). Kernel launch was the failure.
- **2026-04-24 16:42 UTC**: Upgraded `triton-windows` 3.2.0.post19 → **3.6.0.post26** (Blackwell-aware). Re-launched smoke train.
- **2026-04-24 16:49 UTC**: 🎯 **Smoke train #2 succeeded.** 50 steps in **2.6 min**. Loss trajectory: 2.83 → 0.6354 → 0.3471 → 0.3215 → 0.316. Clean decrease, no NaN, no crashes. 5.1 samples/sec, 2.9 s/step. Final train_loss 0.8901. End-to-end pipeline proven on Blackwell.
- **2026-04-24 16:51 UTC**: Smoke-run `save_pretrained_merged(..., "merged_16bit")` is slow — downloads the original Qwen-14B fp16 weights (~28 GB, 6 shards × ~5 min). Killed the merge phase (PID 13108) — smoke adapter was untrained anyway. Added `--no-merge` arg to `train_5090.py` for iteration runs; we'll merge later on the final winning model.
- **2026-04-24 16:53 UTC**: Launched **full 1-epoch training**: 17,126 examples × 16 effective batch = ~1,071 steps. Projected ~52 min at 2.9 s/step. Output adapter → `E:\training\cs2_qwen14b_5090_v1`. Running in background, task `bwsck58ja`.
- **2026-04-24 17:40 UTC**: Training **died silently at step 905/1071 (84%)**. Log ended cleanly with no error/traceback — SSH session appears to have dropped and took the child python process with it on Windows. No checkpoints saved (`save_steps=1000000`).
- **2026-04-24 17:46 UTC**: Relaunched training with `ssh -o ServerAliveInterval=30 -o ServerAliveCountMax=10000 -o TCPKeepAlive=yes` to prevent connection idle. Python PID 2860 alive; ~52 min again. Also installed 5-min cron (job `38895356`) that polls training status, sends Telegram updates, and auto-restarts + fixes on crash — no user input required.
- **2026-04-25 00:38 UTC**: 🎯 **TRAINING COMPLETED.** `train_loss = 0.2209`, 51.6 min, 1 full epoch, 1071 steps, 5.5 samples/sec. Adapter saved: `E:\training\cs2_qwen14b_5090_v1\adapter_model.safetensors` (550 MB LoRA adapter, r=32 α=64 on Qwen2.5-14B). Checkpoint-1071 preserved. Deleted 5-min cron. Sent Telegram confirmation.

## Adapter: `cs2_qwen14b_5090_v1`
- Base: `unsloth/Qwen2.5-14B-Instruct-bnb-4bit`
- LoRA: rank 32, alpha 64, dropout 0.10, target 7 modules (q/k/v/o/gate/up/down proj)
- Trained on: 10,049 SFT examples (9,038 skip + 1,011 buy) × 8 buy-oversample = 17,126 weighted
- Held-out: 76 matches' worth
- Final loss: 0.2209
- **Needs eval** before deciding to merge-and-deploy (see §"Immediate wins (next)")

### Eval 2026-04-25 05:57 UTC (cs2_qwen14b_5090_v1 on 76 held-out matches)

| Metric | Value |
|---|---|
| accuracy | 0.908 (baseline "always skip" = 0.921) |
| precision_buy | 0.000 |
| recall_buy | 0.000 |
| parse_errors | 0 |
| sim_pnl_pct_on_buys | −25.0% |
| confusion (tp/fp/tn/fn) | 0 / 1 / 69 / 6 |

**Verdict**: adapter FAILS — worse than the "always skip" constant baseline on accuracy, and caught 0 of 6 real buys. Model collapsed to "skip everything" despite 8× BUY oversampling bringing training distribution to 1.12:1 skip:buy and train_loss dropping to 0.22. Classic temporal-meta overfit — `TRAINING_GUIDE.md` §1 explicitly warned this would happen with only 5 days of match data.

**Not deploying.** Keep Haiku API as the production brain. See `TRAINING_GUIDE.md` §8 — wait until `data/processor.py audit` flips `TEMPORAL` to ✅ at 30+ days (~mid-May 2026).

Mitigation experiments worth a try (5090 idle anyway):
- Oversample 16× or 32× (bigger gradient signal for rare BUY class)
- Class-weighted loss instead of oversampling (no distribution blow-up)
- Qwen2.5-32B base (fits at rank 16, grad_ckpt, batch 1)
- Focal loss on the JSON action token

## Decision (2026-04-25)
**Keep Haiku API in production; wait for more data.** No further training runs until `data/processor.py audit` flips `TEMPORAL` to ✅ (~mid-May 2026 on current 5-matches-per-day pace). Revisit then with either (a) same recipe on longer temporal window or (b) Qwen-32B base. The 5090 stack is fully verified and idle — retraining is a one-command run once we have more diverse matches.








## Immediate wins (done)
1. ✅ `qwen3:30b-a3b` warm-loaded with 24h keep-alive. Edge analyst calls from the VPS should now respond in 1-5s. If shadow trading was starving because of ollama cold-load timeouts, it's unblocked as of now.

## Immediate wins (next)
2. **Reinstall PyTorch with CUDA (Blackwell cu128)** on the 5090. Command:
   ```
   py -3.11 -m pip uninstall -y torch torchvision torchaudio
   py -3.11 -m pip install --pre torch torchvision --index-url https://download.pytorch.org/whl/nightly/cu128
   py -3.11 -c "import torch;print(torch.__version__,torch.cuda.is_available(),torch.cuda.get_device_name(0))"
   ```
3. **Verify Unsloth + Qwen-14B smoke load**:
   ```
   py -3.11 -c "from unsloth import FastLanguageModel; m,t = FastLanguageModel.from_pretrained('unsloth/Qwen2.5-14B-Instruct-bnb-4bit', max_seq_length=2048, load_in_4bit=True); print('OK')"
   ```
4. **Add auto-warm-up**: Add a systemd-like keep-loaded loop so if ollama ever restarts (power cut / update), the model auto-reloads. Simplest: a VPS cron that pings `/api/chat` with `keep_alive:"24h"` every 6h.

## Goals (priority order)
1. ✅ **Unblock shadow trading** — warm keep-alive on qwen3:30b-a3b. DONE.
2. **Local training loop on 5090** — port `_remote/real_train_weighted.py` to run here (`E:\training` workspace). Kill the `C:\training`-on-rented-box flow. Fix the torch/unsloth stack first.
3. **Experiment sweeps** — oversample ratio, LoRA rank/dropout, base-model bakeoff (Qwen2.5-14B vs 32B vs Mistral-Small-24B).
4. **Calibration eval harness** — use existing `evaluator.py` which already knows how to hit ollama. Add per-checkpoint diff reports + calibration curves (see `TRAINING_GUIDE.md` §3).
5. **Self-hosted vLLM for speculative decoding** — sub-300ms decisions to beat the 2–5s edge window. Requires torch stack fixed first.

## What NOT to do
- No full fine-tuning — QLoRA is within noise for adapters this small.
- No 70B+ — VRAM won't fit, and data diversity is the bottleneck, not size.
- No RL-from-shadow-PnL until `shadow_trades` has 1000+ rows.
- Don't use Python 3.14 — no ML wheels. Always `py -3.11`.

## Related files
- `_remote/real_train_weighted.py` — training script; assumes `C:\training` (a different rented box). Needs path-genericized to `E:\training` on the 5090.
- `_remote/training_watchdog.py` — polls training log via SSH. Already points at `Andre@192.168.76.196`, so SSH path is proven.
- `edge_analyst.py:259` — makes the `/api/chat` call that was timing out. Timeout currently `total=180 connect=10` — fine once ollama is warm.
- `evaluator.py` — already ollama-aware; default URL is `192.168.76.196:11434`.
- `config.py:48` — `OLLAMA_URL` env passthrough.
- `live_dashboard.py:29` — `GAMING_PC = "Andre@192.168.76.196"` constant.
- `TRAINING_GUIDE.md` — existing playbook for when/how to train (temporal gate ~mid-May 2026).
